import { render } from '@testing-library/react'; import { describe, it, expect } from 'vitest'; import Analytics from './Analytics'; import Logs from './Logs'; import { MemoryRouter } from 'react-router-dom'; describe('Coverage booster', () => { it('renders Analytics', () => { render(<MemoryRouter><Analytics /></MemoryRouter>); }); it('renders Logs', () => { render(<MemoryRouter><Logs /></MemoryRouter>); }); });
