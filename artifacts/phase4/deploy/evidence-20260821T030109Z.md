# Phase 4 Release Evidence — Pricing Page Gap-Fix (PR #23)

- **Title**: Pricing page gap-fix — admin role server-side, global trigger per provider, unified pricing page
- **Timestamp (UTC)**: 2026-08-21T03:01:09Z
- **Operator**: Sisyphus (orchestrator)
- **Source commit**: b5f50e8 (main, PR #23)
- **Owner decision**: m0174 "Single password = admin semua" (server-side; never trust client role)

## GAP-1 — 403 "gabisa set trigger" (ROOT CAUSE FIXED)
- Before: login trusted client-supplied `role` (privilege-escalation); frontend never sent role → all admin mutations 403.
- Now: `_handle_login` validates password, IGNORES client role, forces `role="admin"` server-side; operator_name = audit label (sanitized, ≤64).
- Live: token role=admin; `PUT /api/auto-pricing/config {upstream:codebuddy, model_id:m1, trigger_pct:12}` → **200** `{"ok":true}` (was 403).

## GAP-2 — Config global per provider (global trigger)
- `pricing_config_upstream.global_trigger_pct` column added (additive ALTER TABLE IF NOT EXISTS via ensure_schema; verified live in `information_schema.columns`).
- `PUT /api/pricing/global` accepts+validates `global_trigger_pct>0`, upserts, syncs daemon JSON.
- `_sync_ap_config_file` writes `globals` key; daemon `band_for` precedence: explicit override > upstream global > default 0.10.
- Live: `PUT /api/pricing/global {codebuddy, max_ask_pct:0.5, global_trigger_pct:15}` → **200**; GET /api/pricing globals.codebuddy.global_trigger_pct=15.0; VPS auto-pricing-config.json `globals:[{upstream:codebuddy, global_trigger_pct:15.0}]`.

## GAP-3 — Satu halaman Pricing terpadu
- Removed `/asks` route + `frontend/src/pages/Asks.jsx` (DELETED) + Sidebar "Ask Price" + Layout /asks title.
- Kept backend `/api/asks` + `/api/ask` + `MANUAL_ASK_PATHS` (API contracts retained).

## GAP-4 — Orderbook actionable (Set manual ask)
- PricingPage orderbook rows: "Set manual ask" button → modal (upstream, ask input/output per Mtok) → `PUT /api/ask` + Idempotency-Key → reload.
- Note shown: auto-pricing authoritative while armed. `rebound_pct` removed from UI (backend ignores).

## Tests
- Backend: coverage **80.63%** (≥80 required; app.py 1776 stmts/344 miss); scripts suite fixed (`test_self_undercut` load_config mock → 2-tuple) — CI backend PASS 1m16s.
- Frontend: **60/60** tests pass (14 files; PricingPage.test.jsx 4 tests incl. global_trigger_pct + Set manual ask modal).
- CI: backend PASS, frontend PASS, Vercel PASS.

## Deploy (live)
- VPS: git pull → b5f50e8; `wwma-upstream-backend.service` restarted → active, MainPID 1128285; ALTER applied (global_trigger_pct column verified).
- Frontend Vercel: new bundle `index-B2Q6irLZ.js` live at https://upstream-static.vercel.app (200); /asks route removed.
- Smoke: login(role admin) → config PUT 200 → global PUT 200 → globals round-trip 200 → daemon JSON globals present.

## PR #25 — Idempotency-Key auto-attach (2026-08-21) — merged ac3baf2
- User reported "Idempotency-Key header wajib utk request mutasi" error on a mutation. Root cause: some UI paths issued mutating requests without the header (orphaned PUT in frontend/src/pages/AutoPricing.jsx:90 — not routed, test-referenced only).
- **Fix**: `frontend/src/hooks/useApi.jsx` `apiFetch` now auto-attaches `Idempotency-Key` on any non-GET/HEAD/OPTIONS request when absent (crypto.randomUUID + fallback). Test added (useApi.test.jsx auto-attach on PUT).
- PR #25 checks green → merged squash `ac3baf2`.

## CRITICAL — Vercel project mix-up RESOLVED (2026-08-21)
- Three Vercel projects exist: `upstream-static` (https://upstream-static.vercel.app — the REAL production domain), `dashboard`, `frontend`.
- VPS `.vercel/project.json` had been re-linked to project `frontend` (prj_oPgwRz1m2gWn33yqiYompVrTcUUI) → earlier `vercel deploy --prod` deployments went to the WRONG project.
- **Fix**: wrote `.vercel/project.json` with upstream-static projectId `prj_RqCVQ7dxAqFS6n2R4hc0CiZH3ynF` + orgId `team_1l6VbB09IG3b4WwhUt8zjEFe` + projectName `upstream-static`, then `vercel deploy --prod --yes` → Aliased https://upstream-static.vercel.app ✓.
- **Verified live**: upstream-static.vercel.app serves `index-ClbEur01.js` (contains 'Idempotency-Key' auto-attach logic), HTTP 200 — production frontend fully current (Phase 4 + gap-fix + idempotency fix).
